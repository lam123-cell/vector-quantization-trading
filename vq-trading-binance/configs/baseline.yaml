# Baseline Configuration (No Compression)
model:
  type: "DRL" # Options: DRL, LSTM
  params:
    learning_rate: 0.0001
    batch_size: 64

data:
  symbol: "BTC/USDT"
  timeframe: "1h"
  quantization: false # No compression
